package com.example.one_card;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

public class ProfileActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        //Intent intent=getIntent();
        //int position=intent.getIntExtra("position", 0);

        /*if(position==0){setContentView(R.layout.activity_visit); setTitle("카페 방문내역");}
        else if(position==1){setContentView(R.layout.activity_review); setTitle("리뷰관리");}
        else if(position==2){setContentView(R.layout.activity_like); setTitle("관심카페등록");}
        else{setContentView(R.layout.activity_profile); setTitle("아이디/계정관리");}*/
    }
}
